---
marp: true
title: Example Custom Syntax
theme: default
---

# Custom Code Block Example

This demonstrates how to use the Jinja2 template system with custom syntax.

## Standard Code Block

{{ generate_code_block('python', 'print("Hello, World!")') }}

## Using Your Custom Syntax (Example)

You can extend the system to support your own syntax. For example:

{{ custom_syntax_block('my-special-syntax', 'your custom code here') }}

## Advanced Examples

### With Highlighting
{{ generate_highlighted_code_block('python', 'def hello():\n    print("Hello")\n    return True', [2]) }}

### Terminal Session
{{ generate_terminal_block('ls -la', 'total 8\ndrwxr-xr-x 2 user user 4096 Jan 1 12:00 .') }}

### Different Auto-scaling
{{ generate_code_block('yaml', 'apiVersion: v1\nkind: Service', 'both') }}

## Your Custom Function

When you're ready to implement your custom syntax function, you would:

1. Add it to `code_generator.py`
2. Register it in `render_template.py`
3. Use it like: `{{ your_function_name(parameters) }}`
